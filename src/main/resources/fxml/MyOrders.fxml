<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx/21"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.matraknhash.ui.controller.MyOrdersController"
      spacing="14" styleClass="page-root">
    <padding><Insets top="20" right="24" bottom="20" left="24"/></padding>

    <HBox alignment="CENTER_LEFT" spacing="10">
        <Label text="My Orders" styleClass="page-h1"/>
        <Region HBox.hgrow="ALWAYS"/>
        <Button text="Refresh" onAction="#onRefresh" styleClass="btn-secondary"/>
    </HBox>

    <SplitPane dividerPositions="0.42" VBox.vgrow="ALWAYS">
        <items>
            <VBox spacing="8">
                <Label text="Your purchases" styleClass="section-title"/>
                <TableView fx:id="orderTable" VBox.vgrow="ALWAYS">
                    <columns>
                        <TableColumn fx:id="colId"     text="Order"   prefWidth="80"/>
                        <TableColumn fx:id="colSeller" text="Seller"  prefWidth="140"/>
                        <TableColumn fx:id="colTotal"  text="Total"   prefWidth="100"/>
                        <TableColumn fx:id="colStat"   text="Status"  prefWidth="130"/>
                        <TableColumn fx:id="colWhen"   text="Placed"  prefWidth="140"/>
                    </columns>
                </TableView>
            </VBox>

            <VBox spacing="10">
                <Label fx:id="detailHeader" text="Pick an order from the left →" styleClass="section-title"/>
                <TableView fx:id="lineTable" VBox.vgrow="ALWAYS">
                    <columns>
                        <TableColumn fx:id="colLname" text="Part"     prefWidth="220"/>
                        <TableColumn fx:id="colLqty"  text="Qty"      prefWidth="60"/>
                        <TableColumn fx:id="colLprc"  text="Unit"     prefWidth="90"/>
                        <TableColumn fx:id="colLsub"  text="Subtotal" prefWidth="100"/>
                    </columns>
                </TableView>

                <Label fx:id="detailFooter" text="" styleClass="muted" wrapText="true"/>

                <!-- Return section -->
                <VBox fx:id="returnBox" spacing="6" managed="false" visible="false">
                    <Label text="Return reason:" styleClass="muted"/>
                    <TextArea fx:id="returnReason" prefRowCount="2" promptText="Why are you returning this order?"/>
                </VBox>

                <HBox spacing="10" alignment="CENTER_RIGHT">
                    <Button fx:id="btnReturn" text="📦  Request Return" onAction="#onRequestReturn"
                            styleClass="btn-danger" disable="true"/>
                    <Button fx:id="btnRate" text="⭐  Rate Seller" onAction="#onRateSeller"
                            styleClass="btn-primary" disable="true"/>
                </HBox>

                <Label fx:id="status" wrapText="true" styleClass="muted"/>
            </VBox>
        </items>
    </SplitPane>
</VBox>
